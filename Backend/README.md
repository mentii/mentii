# Backend

To run the flask app:

Make sure google app engine is installed and the location of the dev_appserver.py executable is in your PATH. 
Instructions can be found here:

https://cloud.google.com/appengine/docs/python/quickstart

Once installed, you can run the app locally by executing the run.sh script:

./run.sh

The content will be displayed at:

localhost:8080

